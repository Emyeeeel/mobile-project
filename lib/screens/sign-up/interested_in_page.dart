import 'package:flutter/material.dart';

class InterestedInPage extends StatelessWidget {
  const InterestedInPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text('Interested in Page'),),
    );
  }
}